# Playwright-Ticket-Pitch-Automation
Automated Playwright test suite for PitchToMatch that logs in users, purchases tickets with random quantities, applies promo codes, joins events, and submits pitches from CSV data. Runs tests in parallel for multiple accounts with validation and reporting.
